{
  "name": "lite-tstruct-builder",
  "private": true,
  "description": "Lite Tstruct Builder: a React + Vite app / embeddable library (web/) that stores its data in the browser. There is no backend.",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:web": "npm --prefix web run dev",
    "build:web": "npm --prefix web run build",
    "build:lib": "npm --prefix web run build:lib",
    "e2e": "npm --prefix e2e run test"
  }
}
